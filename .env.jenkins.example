# Copy to .env.jenkins (gitignored). Read by jenkins/casc.yaml at
# container startup to create the initial admin user -- see the
# "CI/CD (Jenkins)" section of the README.

JENKINS_ADMIN_ID=admin
JENKINS_ADMIN_PASSWORD=changeme
